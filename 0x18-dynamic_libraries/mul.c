# include "main.h"

/**
* mul - returns multiplication of two integers
* @a: first integer
* @b: second integer
* Return: multiplication of two integers
*/

int mul(int a, int b)
{
	return (a * b);
}
