# include "main.h"

/**
  *add - Entry point
  *@a: first integer
  *@b: second integer
  *Description: adds two integers and returns the result
  *Return: Total of two integers
  */

int add(int a, int b)
{
	int total;

	total = (a + b);
	return (total);
}
