#include "main.h"
#include <stdio.h>

/**
* main - prints its name (program name)
* @argc: represents the number ofcommand line arguments
* @argv: name of the array
* Return: Always success (0)
*/

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);

	return (0);
}
